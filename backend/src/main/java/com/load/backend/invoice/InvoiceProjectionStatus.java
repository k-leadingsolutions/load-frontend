package com.load.backend.invoice;

/** Mirrors {@code com.load.backend.order.InvoiceStatus} for the persisted invoice projection. */
public enum InvoiceProjectionStatus {
    NOT_AVAILABLE,
    READY
}
